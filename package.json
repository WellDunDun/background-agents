{
  "name": "flue-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "flue build --target cloudflare",
    "dev": "flue dev --target cloudflare",
    "deploy": "npm run build && wrangler deploy --config dist/flue_factory/wrangler.json",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run --config dist/flue_factory/wrangler.json",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "@daytona/sdk": "0.187.0",
    "@earendil-works/pi-ai": "0.79.6",
    "@flue/runtime": "1.0.0-beta.1",
    "agents": "^0.14.1",
    "hono": "4.12.23",
    "valibot": "1.4.1"
  },
  "devDependencies": {
    "@flue/cli": "1.0.0-beta.1",
    "typescript": "^5.9.3",
    "wrangler": "4.73.0"
  }
}
